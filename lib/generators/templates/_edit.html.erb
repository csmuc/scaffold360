<div class="<%= singular_name %>_remote_target">
	<h1>Editing <%= singular_name %></h1>
  <%%= render 'form', :remote => request.xhr? ? {:replace=>'.<%= singular_name %>_remote_target'} : false %>
</div>